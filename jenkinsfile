pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps { 
                git branch: 'main', url: 'https://github.com/PabloEscales/iac-petclinic.git'
            }
        }

        stage('Versions and T init') {
            steps {
                sh 'terraform --version'
                dir('terraform') {
                    sh 'terraform init'
		    sh 'terraform apply -auto-approve'
                }
            }
        }
    }
}
